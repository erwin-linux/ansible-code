name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u erwinn bash -c 'cat > /home/erwinn/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/erwinn/ansible/inventory.yaml
          roles_path    = /home/erwinn/ansible/roles
          host_key_checking = False
          remote_user = erwinn
          private_key_file = /home/erwinn/.ssh/id_rsa
          log_path = /home/erwinn/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/erwinn/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/erwinn/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown erwinn:erwinn /home/erwinn/ansible/ansible.cfg
          sudo chmod 644 /home/erwinn/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u erwinn ls -la /home/erwinn/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u erwinn cat /home/erwinn/ansible/ansible.cfg

